# Testcontainers Configuration for macOS with Docker Desktop

# Docker socket on macOS  
# This helps Testcontainers auto-detect Docker Desktop
docker.client.strategy=unix

#  Enable ryuk container cleanup (set to false to keep containers for debugging)
ryuk.container.privileged=true

# Log level for debugging
logback.appenders=STDOUT
